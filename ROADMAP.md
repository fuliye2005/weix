# Weix 未来规划

本文档记录后续开发方向，按可靠性和实际使用价值排序。所有自动发送相关功能都应优先保证“不误发、不重复发、可追踪”，再考虑提高自动化程度。

## 第一阶段：发送可靠性

目标：解决“输入成功但没有点击发送”、发送失败难以定位和自动重试可能重复发送的问题。

- 为每次出站消息生成稳定的幂等键，至少包含目标账号、会话、消息内容和触发消息 ID。
- 发送前检查短时间窗口内是否已经存在相同目标和内容的发送尝试；已确认发送的消息直接跳过。
- 区分“未执行发送动作”和“已执行但尚未确认”两类失败。只有前者允许自动重试，后者先查询微信数据库或等待确认，禁止盲目再次发送。
- 为每次尝试记录阶段、发送方式、重试次数、错误码、耗时和最终结果，并在管理后台提供筛选。
- 增加发送前、点击发送后和最终确认后的诊断截图；截图默认只保存在本机日志目录，并提供保留天数和脱敏开关。
- 在不发送真实消息的前提下增加 UIA 模拟测试；真实联系人端到端测试继续保持手动确认。

## 第二阶段：通用业务回复

目标：保持通用聊天能力，同时让代肝咨询回复稳定、可控。

- 继续使用提示词作为主要业务规则入口，不把机器人限制成专用客服。
- 支持按联系人或群聊配置业务提示词片段、服务说明、工作时间和人工接管规则。
- 增加“人工接管”“暂停自动回复”“恢复自动回复”等明确命令。
- 对价格、排队、订单进度、库存和交付时间等高风险信息设置“不确定就转人工/等待确认”的默认策略。
- 为联网搜索增加超时、失败重试、短期缓存和隐私字段过滤；搜索只发送必要的公开问题。

## 第三阶段：知识与历史话术

目标：让 JSON 聊天记录学习更稳定，减少错误拼接和不合适复用。

- 导入时强化目标人物识别、会话隔离、时间顺序和消息类型过滤。
- 为历史话术增加来源追踪和可删除的样本管理，避免撤回内容、系统消息或其他人物话术进入 Persona。
- 继续优化 `persona`、`replay`、`hybrid` 三种模式的阈值和重复冷却。
- 增加离线评测集，比较回复相关性、风格相似度、重复率和业务事实准确性。

## 第四阶段：部署与多账号

目标：让程序更容易迁移到其他 Windows 设备，并安全支持多个微信账号。

- 增加一键诊断：Python/UIA 运行时、微信版本、账号绑定、数据库密钥、API Key、搜索和发送能力。
- 提供配置导出/导入，但默认排除 API Key、微信数据库密钥、聊天记录和本地向量库。
- 增加安全的备份与恢复，以及配置变更审计。
- 支持账号列表、账号状态、账号级白名单、账号级提示词和账号级发送策略。
- 在稳定性验证完成后，再考虑将桌面发布包做成自动更新版本。

## 暂不优先

- 不急于训练或微调本地大模型；当前云端模型 + 提示词 + Persona/RAG 已能覆盖主要需求。
- 不默认启用坐标鼠标发送；UIA 发送和确认链路稳定前，鼠标兜底保持显式配置。
- 不把每条消息都交给联网搜索，避免增加延迟、泄露隐私和引入不必要的不确定信息。

## 发布原则

- 任何发送逻辑改动都必须有单元测试、失败路径测试和 `git diff --check` 检查。
- 涉及真实微信账号的测试必须使用白名单和测试联系人，并在测试前明确确认。
- 发布包只包含模板和程序代码；个人配置、密钥、数据库、聊天记录和向量数据留在本机。
